
<a class="course-contact" href="{{{url}}}" data-placement="top" data-toggle="tooltip" title="{{{name}}}">
    <img class="rounded-circle course-contact-icon" src="{{picture}}">
    <span class="sr-only">{{{name}}}</span>
</a>